export default function RootPage() {
  // This page has been moved to /src/app/(main)/page.tsx 
  // to ensure it uses the main application layout with navigation.
  // The AuthProvider in the root layout will automatically handle 
  // redirecting users to the login page or the main dashboard.
  return null;
}
